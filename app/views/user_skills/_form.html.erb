<%= form_with(model: user_skill, local: true) do |form| %>
  <% if user_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_skill.errors.count, "error") %> prohibited this user_skill from being saved:</h2>

      <ul>
      <% user_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_profile_id %>
    <%= form.text_field :user_profile_id, id: :user_skill_user_profile_id %>
  </div>

  <div class="field">
    <%= form.label :skill_id %>
    <%= form.text_field :skill_id, id: :user_skill_skill_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
