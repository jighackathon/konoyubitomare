<%= form_with(model: chat_message, local: true) do |form| %>
  <% if chat_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat_message.errors.count, "error") %> prohibited this chat_message from being saved:</h2>

      <ul>
      <% chat_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :chat_message_project_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :chat_message_user_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :chat_message_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
